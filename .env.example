APP_NAME="Campus Virtual"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mongodb
DB_HOST=127.0.0.1
DB_PORT=27017
DB_DATABASE=campus_virtual
# DB_USERNAME=root
# DB_PASSWORD=
MONGODB_DSN=mongodb://127.0.0.1:27017/?replicaSet=rs0

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

CACHE_STORE=file
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

OAUTH2_ISSUER="${APP_URL}"
OAUTH2_AUDIENCE=campus-virtual-services
OAUTH2_ACCESS_TOKEN_TTL=3600
# Use a separate secret in shared environments. Defaults to APP_KEY for local development.
# OAUTH2_SIGNING_KEY=

# Publicador de eventos intermodulares
# EVENTS_SINK_URL=https://events.example.internal/v1/events
# EVENTS_SINK_TOKEN=
EVENTS_SINK_TIMEOUT=10
EVENTS_PUBLISH_BATCH=100

# Modulo 1.6 - QR dinamico: segundos de vigencia de cada token rotativo
QR_IDENTITY_TTL_SECONDS=30
# Use a dedicated random secret (at least 32 characters) for indexed QR lookups.
# Until set, config/qr.php explicitly uses APP_KEY for compatibility.
# QR_LOOKUP_KEY=

# Modulo 1.7 - Reautenticacion: minutos que dura vigente una confirmacion
# de contrasena para poder ejecutar acciones sensibles (cerrar sesiones
# remotas, revocar dispositivos, etc.)
REAUTH_VALID_MINUTES=5

# Modulo 1.7 - Maximo de sesiones activas simultaneas por estudiante.
# Al superarse, se revocan automaticamente las sesiones activas mas
# antiguas (no la que se acaba de crear). 0 desactiva el limite.
MAX_ACTIVE_SESSIONS_PER_USER=5

# Modulo 1.7 - Dias de retencion de la bitacora de seguridad
# (security_events) antes de que el comando "security-events:prune"
# (agendado a diario) la elimine.
SECURITY_EVENTS_RETENTION_DAYS=90
